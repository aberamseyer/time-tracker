<%
  const list = include('partials/task-list', { tasks });
  const body = `
    <div class="tasks-head">
      <h1>Tasks</h1>
      <button class="add-task" hx-post="/tasks" hx-target="#task-list" hx-swap="outerHTML">+ New task</button>
    </div>
    ${list}`;
%>
<%- include('layout', { title, nav, body }) %>
