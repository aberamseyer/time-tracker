<%
  const panel = include('partials/analytics-panel', { p, report, rangeLabel, tasks, tags, fmtDuration, fmtMoney });
  const body = `<h1>Analytics</h1>${panel}`;
%>
<%- include('layout', { title, nav, body, wide: true }) %>
